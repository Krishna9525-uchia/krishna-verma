## 2025-05-15 - [Label-Input Association]
**Learning:** In React applications with dynamic forms, failing to associate labels with their inputs using `htmlFor` and `id` prevents screen readers from announcing input context and disables label-click focus.
**Action:** Always ensure dynamic form mappings include matching `htmlFor` on labels and `id` on the corresponding input/select elements.
